---
---

<div class="bg-white dark:bg-gray-800 p-6 md:p-8 rounded-lg shadow-md" x-data="{ 
	phone: '', 
	submitted: false,
	loading: false,
	submitForm() {
		this.loading = true;
		setTimeout(() => {
			this.submitted = true;
			this.loading = false;
			this.phone = '';
			setTimeout(() => this.submitted = false, 3000);
		}, 1000);
	}
}">
	<h2 class="mt-0 mb-6 text-[#1a1a2e] dark:text-white">Заказать обратный звонок</h2>
	<form @submit.prevent="submitForm()" x-show="!submitted">
		<div class="mb-6">
			<label for="callback-phone" class="block mb-2 text-gray-700 dark:text-gray-300 font-medium">Ваш телефон</label>
			<input 
				type="tel" 
				id="callback-phone"
				x-model="phone"
				placeholder="+7 (___) ___-__-__"
				required
				class="w-full px-3 py-3 border-2 border-gray-200 dark:border-gray-600 dark:bg-gray-700 dark:text-white rounded-md text-base transition-colors focus:outline-none focus:border-[#4a9eff]"
			/>
		</div>
		<button 
			type="submit" 
			:disabled="loading" 
			class="px-8 py-3 border-none rounded-md text-base cursor-pointer transition-all bg-[#4a9eff] text-white hover:bg-[#357abd] disabled:opacity-60 disabled:cursor-not-allowed"
		>
			<span x-show="!loading">Заказать звонок</span>
			<span x-show="loading">Отправка...</span>
		</button>
	</form>
	<div x-show="submitted" class="bg-green-100 text-green-800 p-4 rounded-md text-center">
		<p>Спасибо! Мы свяжемся с вами в ближайшее время.</p>
	</div>
</div>
