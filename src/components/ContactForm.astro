---
---

<div class="bg-white dark:bg-gray-800 p-6 md:p-8 rounded-lg shadow-md" x-data="{ 
	name: '', 
	email: '',
	message: '',
	submitted: false,
	loading: false,
	submitForm() {
		this.loading = true;
		setTimeout(() => {
			this.submitted = true;
			this.loading = false;
			this.name = '';
			this.email = '';
			this.message = '';
			setTimeout(() => this.submitted = false, 3000);
		}, 1000);
	}
}">
	<h2 class="mt-0 mb-6 text-[#1a1a2e] dark:text-white">Форма обратной связи</h2>
	<form @submit.prevent="submitForm()" x-show="!submitted">
		<div class="mb-6">
			<label for="contact-name" class="block mb-2 text-gray-700 dark:text-gray-300 font-medium">Ваше имя</label>
			<input 
				type="text" 
				id="contact-name"
				x-model="name"
				placeholder="Иван Иванов"
				required
				class="w-full px-3 py-3 border-2 border-gray-200 dark:border-gray-600 dark:bg-gray-700 dark:text-white rounded-md text-base font-inherit transition-colors focus:outline-none focus:border-[#4a9eff]"
			/>
		</div>
		<div class="mb-6">
			<label for="contact-email" class="block mb-2 text-gray-700 dark:text-gray-300 font-medium">Email</label>
			<input 
				type="email" 
				id="contact-email"
				x-model="email"
				placeholder="ivan@example.com"
				required
				class="w-full px-3 py-3 border-2 border-gray-200 dark:border-gray-600 dark:bg-gray-700 dark:text-white rounded-md text-base font-inherit transition-colors focus:outline-none focus:border-[#4a9eff]"
			/>
		</div>
		<div class="mb-6">
			<label for="contact-message" class="block mb-2 text-gray-700 dark:text-gray-300 font-medium">Сообщение</label>
			<textarea 
				id="contact-message"
				x-model="message"
				rows="5"
				placeholder="Ваше сообщение..."
				required
				class="w-full px-3 py-3 border-2 border-gray-200 dark:border-gray-600 dark:bg-gray-700 dark:text-white rounded-md text-base font-inherit transition-colors focus:outline-none focus:border-[#4a9eff]"
			></textarea>
		</div>
		<button 
			type="submit" 
			:disabled="loading" 
			class="px-8 py-3 border-none rounded-md text-base cursor-pointer transition-all bg-[#4a9eff] text-white hover:bg-[#357abd] disabled:opacity-60 disabled:cursor-not-allowed"
		>
			<span x-show="!loading">Отправить</span>
			<span x-show="loading">Отправка...</span>
		</button>
	</form>
	<div x-show="submitted" class="bg-green-100 text-green-800 p-4 rounded-md text-center">
		<p>Спасибо за ваше сообщение! Мы ответим вам в ближайшее время.</p>
	</div>
</div>
